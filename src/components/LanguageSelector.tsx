import React from 'react';
import { useTranslation } from '@/contexts/LanguageContext';

// This component is no longer used as we've moved the language selector to the header
const LanguageSelector: React.FC = () => {
  return null; // This component is kept for backward compatibility
};

export default LanguageSelector;
